html
    style
        include ../styles/style.css
    header
        a(href='/api/jokes') Jokes
        br
        a(href='/api/othersites') Andre sites

        title Jokes
        meta(charset="UTF-8")
    body
    div(class=("box"))
        div(class=("content"))
            h1(id=("jokes1")) Jokes fra anden site
    table(id= "jokestabel")
        each side in sider
            tr
                td #{side.name}
                td#othercolA
                    a(href="otherjokes/" + side.name) Jokes      
                    a(href=side.address)  Site
                    
    br

    //---------------------------
    //- div
    //-     each jokeSpSite in enkelt
    //-         p Setup: #{jokeSpSite.setup}
    //-         p Punchline: #{jokeSpSite.punchline}
    //-         br

    
    -// each joke in jokes
    -// mangler at køre igennem alle jokes der er lavet i forvejen
    -// og sætte det i en ul(unordered list)

